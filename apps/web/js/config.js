/**
 * config.js — Configuración del frontend.
 *
 * Sin build step (sin Vite/webpack), así que no hay variables de entorno
 * en tiempo de compilación: esto es simplemente una constante que se edita
 * a mano al desplegar a otro entorno.
 */

export const API_BASE_URL = 'http://localhost:3001/api';
